name: ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

jobs:
  build:
    name: build + test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup .NET
        uses: actions/setup-dotnet@v4
        with:
          dotnet-version: '10.0.x'

      - name: Restore
        run: dotnet restore Shf.Cli.slnx

      - name: Build
        run: dotnet build Shf.Cli.slnx --configuration Release --no-restore

      - name: Test
        run: dotnet test Shf.Cli.slnx --configuration Release --no-build --verbosity normal
